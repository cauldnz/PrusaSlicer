name: Build osx

# macOS runners are ~10x Linux cost — build only on a manual workflow_dispatch.
on:
  workflow_dispatch:

jobs:
  build_osx:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/build_osx.yml@legacy
    secrets: inherit
    with:
      actions_branch: legacy
